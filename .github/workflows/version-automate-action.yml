name: Version increment

on:
  pull_request:
    types: [closed]
    
    
jobs:
  build:
    if: github.event.pull_request.merged == 'true'




    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Version Control on PR merge
      run: ".github/read_commit.sh"
